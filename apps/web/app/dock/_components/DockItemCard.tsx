/**
 * DockItemCard — 旧版 Dock 卡片组件（已废弃）
 *
 * 新版 Dock 使用 workspace/page.tsx 中的 DockView 组件
 * 该组件为高密度 worklist 表格布局，不再使用独立卡片。
 *
 * To-do: 后端 DockItem 类型仍可复用，需在新 DockView 中接入
 * 相关 API: listDockItems(), archiveItem(), ignoreItem() from @/lib/repository
 */

export {}